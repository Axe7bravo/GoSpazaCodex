# Build-time public configuration. Use APP_ENV=staging for staging builds.
NODE_ENV=production
APP_ENV=production
NEXT_PUBLIC_API_URL=https://api.example.com

# Native Medusa publishable API key required by /store routes (public, not a secret).
NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=
